# ZeroTrace detection rule configuration

Generic API Key=enabled
Generic API Token=enabled
Secret Key=enabled
Password=enabled
Credential=enabled

AWS Access Key=enabled
GitHub Token=enabled
JWT=enabled
Private Key=enabled
# Custom rule
custom|Internal Token|Detects internal application tokens|MYAPP_[A-Za-z0-9]{12,}|90
